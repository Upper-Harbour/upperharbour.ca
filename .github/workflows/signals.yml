# .github/workflows/signals.yml
# Runs the signals intelligence pipeline once daily
# Auto-publishes signals directly to signals.json

name: Signals Pipeline

on:
  schedule:
    # Once daily at 7am ET (noon UTC)
    - cron: '0 12 * * *'
  workflow_dispatch:  # Allow manual trigger

jobs:
  collect:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install anthropic feedparser requests

      - name: Run signals pipeline
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          APPROVAL_EMAIL: ${{ secrets.APPROVAL_EMAIL }}
          SIGNALS_JSON_PATH: signals.json
        run: python signals_pipeline.py collect

      - name: Commit published signals
        run: |
          git config user.name "Signals Bot"
          git config user.email "bot@upperharbour.ca"
          if [ -f signals.json ]; then
            git add -f signals.json
          fi
          if [ -f db-alerts.json ]; then
            git add -f db-alerts.json
          fi
          git diff --staged --quiet || git commit -m "signals: $(date -u +%Y-%m-%dT%H:%M) — auto-published"
          git pull --rebase origin main || true
          git push || true
